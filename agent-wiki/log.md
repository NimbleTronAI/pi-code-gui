# Wiki Log

Append-only chronological record of all wiki operations. Every entry
starts with `## [YYYY-MM-DD] <action> | <description>`.

## [2026-08-06] update | renderInline link-href guard consolidated — `safeLinkHref` (in `src/shared/sanitize.ts`) is gone; `renderInline` now uses `safeInlineLinkHref` in `src/shared/webview-nav-guard.ts`, which shares `ALLOWED_URL_SCHEMES` with `safeExternalUrlString` so the openUrl and renderInline paths never drift on which absolute schemes are allowed. `ftp` is dropped from the allowlist (it was allowed only on the renderInline path before — exactly the drift the shared set closes); both paths now allow `http`/`https`/`mailto` only, plus scheme-less refs (relative/anchor) on renderInline. The 21 `safeLinkHref` tests folded into `src/test/unit/webview-nav-guard.test.ts` (the branch's `src/test/sanitize.test.ts` lived outside the `test:unit` glob and never ran in CI). webview-frontend.md and component-system-proposal.md updated: the "`html` tagged template eliminates the entire class of HTML injection" claim is now scoped to the string-built DOM, with renderInline's `html`/`link` tokens called out as a separately-hardened path. `safeWorkspaceFilePath` was split out of `webview-nav-guard.ts` into a new `src/shared/workspace-path-guard.ts` so `webview-nav-guard.ts` stays browser-safe (no `node:path`) — the webview bundle (esbuild.webview.js, platform browser) now imports it via renderInline's `safeInlineLinkHref`, and a `node:` import would break that bundle. Tests 557, types/lint green.

## [2026-08-05] update | Wiki brought back to complete + correct. AUDIT FIRST: automated scans were near-useless — a dead-file-reference scan found 0 across 20 pages, and a vanished-symbol scan produced 3 hits that were ALL false positives (`restoreActiveSession`/`restoreAdditionalSessions` named in prose explaining they do NOT exist; `CustomMessageComponent` is an upstream TUI concept; `LICENSE` exists at root). The real rot was only findable by reading, because it took the form of pages describing responsibilities that had MOVED while every symbol they named still existed. (1) `pi-service.md` REWRITTEN — it was 7 weeks stale (2026-06-21) and actively misleading: it described PiService as owning "runtime/SDK resolution, agent session creation, event translation, model cycling, thinking level management, and usage stat tracking" (all extracted), named `setEffort` (DELETED), `PiService.formatModelDetail()` (now `model-picker.ts`), `authStorage`/`modelRegistry` (replaced by `ModelRuntime`, surviving only in a comment recording their removal), and `_agentRunActive` as "still on PiService" (now owned by the backends via `getAgentRunActive()`). New version documents what PiService owns, an explicit table of what MOVED and where, and an honest inventory of where the seam still leaks. (2) `pi-backend.md` NEW — the seam and capability model, the CENTRAL abstraction, previously mentioned on exactly 1 page. Covers the primitive/orchestration split, why `thinkingLevelLive()` is a method and `max` a version function rather than flags, the four known leaks (init/dispose identity branching, `extension.ts` reaching past via `sessionManagerInstance`, cost policy keyed on runtime, the webview's un-transmitted capability list), and how to add a capability. (3) `pure-core-modules.md` NEW — 29 modules had NO mention anywhere in the wiki. Documents the extract-the-decision idiom that produced 549 headless tests and indexes all 44 modules (pure cores with what they decide and what they came from, effectful modules, and `test-fixture.ts` as deliberately not application code). (4) `session-window.md` Restore step rewritten — it documented a `workspaceState` loop that had ALREADY been replaced by the `WebviewPanelSerializer` + pure `planPanelRestore()` path (open/fresh/dispose), a replacement whose own code comment says the old approach raced VS Code and double-restored windows. (5) `index.md` refreshed (was a month stale). VERIFIED, not assumed: 0 src modules now unmentioned (was 29), 0 broken internal links (the 4 flagged are illustrative placeholders in wiki-maintenance.md), all 44 modules named in the new pages exist, and all 37 symbols the new/edited pages claim exist in src. wiki:lint clean in full mode. LESSON: a wiki linter that checks structure will report "clean" on a page that is entirely wrong; freshness must be checked against the code, by reading.

## [2026-08-05] update | Dual-runtime review round CLOSED — the one HIGH finding is DISPROVEN; six real bugs found by USING the product, not reviewing it. (1) DISPROVEN — "Rust wipes usage/cost to $0 after an abort" (Rust review #1, HIGH, marked CONFIRMED with a wire probe). The MECHANISM is real: `applyUsage` assigns `this.usage = {...}` wholesale, and the mid-turn guard `if (this._agentRunActive) return` is already cleared by the time `agent_end` fires, so a zeroed stats reply WOULD replace the live accumulate. But the PREMISE is false on 0.1.23: probing `get_session_stats` on a real 121-message session returned `{input:302004, output:39455, cacheRead:5702144}`, not zeros — and the user then aborted turns twice and saw no cost-chip problem. Do NOT "fix" this speculatively; a merge-don't-replace guard would mask legitimate corrections for a bug that does not reproduce. Re-open only with a probe showing zeros AND a visible $0 chip. (2) DISPROVEN — "`/new` CRASHES under Rust via `this.session.agent.waitForIdle()` on a null session" (Rust review #1 of the second pair, CRITICAL, CONFIRMED). It cannot: `newSession()` opens with `if (!this.session) { dispose(); initialize({fresh:true}); return; }` — a guard the review QUOTED in its own evidence block and reasoned straight past. (3) But `/new` WAS broken, in a way no review found: `sessionReset` is declared in protocol.ts AND fully handled in the webview (`resetChat()` clears DOM/tool blocks/bash blocks/history) and NOTHING ever sent it — so /new disposed and recreated the session while the old conversation stayed on screen, indistinguishable from being ignored. Found by the user typing /new. Both runtimes. (4) SELF-REPORTS WERE OUR BUG: a reviewer inspected `ps`, found ANOTHER session's rust-pi in the same workspace, and reported itself as Rust while running the in-process SDK — invalidating everything it framed as first-hand evidence. From inside a session, workspace/session-dir/process-table are identical across backends. Both backends now inject an authoritative identity block from ONE shared builder (`runtime-identity.ts`): TS via `systemPromptOverride` (re-evaluated per turn, so a mid-session model switch is reflected), Rust via `--append-system-prompt` (verified the 0.1.23 binary accepts the multi-line arg). It forbids inferring runtime from ps/filesystem and prescribes "say you cannot determine it". (5) REVIEWS RIGHT ON DIAGNOSIS, WRONG ON REMEDY — Auto-retry: both reviews called the TS toggle a no-op (correct: `setAutoRetry` was an empty method, so the DEFAULT runtime lied about re-billing) and both assumed no SDK setter existed, recommending we hide or capability-gate it. `AgentSession.setAutoRetryEnabled` DOES exist (pi-coding-agent 0.83.0, core/agent-session.d.ts:549 → SettingsManager.setRetryEnabled), on the same class as the `setAutoCompactionEnabled` already in use. Implementation gap, not architecture limit — wired, not removed. Same commit seeds both settings flags from the session's real getters instead of hardcoded `true` (a resumed session showed toggles contradicting its own behaviour; Rust never had the gap, applyState syncs from get_state). (6) LESSON: across 5 reports the two CONFIRMED high-severity findings were both false, while every genuine user-facing bug this round — /new inert, the failed-session catch-22, the login dropdown never dismissing, a dead session refusing to restart after login — came from driving the product. Weight review findings by whether the reviewer OBSERVED the behaviour or inferred it from code shape. Tests 549, types/lint green.

## [2026-07-29] update | `max` ACTIVATED — generator migrated to pi-ai's new API, catalog regenerated, and a backend version gate added. Supersedes the previous entry's "catalog regen impossible" blocker. (1) GENERATOR MIGRATED: pi-ai 0.81.0 removed the module-level `getProviders()`/`getModels()` (`scripts/gen-model-registry.mjs` was built on them; 0.82.1 fails at import). The built-in catalog moved behind `@earendil-works/pi-ai/providers/all` → `getBuiltinProviders()` / `getBuiltinModels(id)`, with the per-model field shape UNCHANGED (baseUrl, api, reasoning, contextWindow, maxTokens, input, cost, thinkingLevelMap, compat) — so only the enumeration moved, the slimming logic is untouched. (2) REGEN AUDIT 0.79.8→0.82.1: providers 28→30 (qwen-token-plan, qwen-token-plan-cn added, NONE dropped), models 756→854 (all 854 keep cost), reasoning 549→659, xhigh keys 75→143, **max keys 0→131** (67 non-null). (3) THE ISSUE'S CASE IS NOW IN OUR DATA: `moonshotai-cn/kimi-k3` maps `{off:null,minimal:null,low:"low",medium:null,high:"high",xhigh:null,max:"max"}` — `xhigh` EXPLICITLY null, so its top tier is reachable ONLY as `max`; `deepseek/deepseek-v4-pro` has no `xhigh` key at all. Models mapping `max:null` (Qwen3.5) correctly don't offer it. (4) NEW LANDMINE FOUND AND CLOSED — the previous safety was accidental ("the catalog has no max keys"), and regenerating destroyed it. Measured BLACK-BOX against the installed pre-#139 binary 0.1.20: a `max` key in models.json is harmless (catalog parses, model lists, no stderr), but `set_thinking_level("max")` returns `Validation error: Invalid thinking level: max` — byte-identical to the rejection a garbage level gets — while `set_thinking_level("xhigh")` succeeds and silently clamps to `high`. So a per-model mapping says the MODEL has the tier and says NOTHING about the backend. New pure `rustHonorsMaxThinkingLevel(version)` in model-catalog.ts gates on `MIN_RUST_VERSION_FOR_MAX = "0.1.23"`, compares the numeric triple only (--version also carries hash+timestamp), and FAILS CLOSED on absent/unparseable. Gated on the DETECTED binary, not the PINNED one — they diverge right now (see below). TS is exempt: it runs through pi-coding-agent 0.82.1 (bundling post-#139 pi-ai), which clamps rather than rejecting. Verified end-to-end: kimi-k3 → `[low, high, max]` on v0.1.23+, `[low, high]` on 0.1.20. (5) DOCS no longer deferred (max is genuinely reachable now): README "off → xhigh" → "off → max"; CHANGELOG entry added. STILL OPEN: v0.1.23 shipped only 5 assets vs v0.1.22's 13 — **no `pi-linux-arm64.tar.xz`** (also missing darwin-amd64, all build manifests, release_gate_report.json), so this aarch64 box cannot install it and still runs 0.1.20; the managed installer now HARD-FAILS for arm64 Linux users ("Release v0.1.23 has no asset pi-linux-arm64.tar.xz") — consider reverting the pin to v0.1.22 until upstream publishes a complete release. Consequence: #136's EBADF fix remains UNVERIFIED here, and `max` is withheld on this machine BY DESIGN until a v0.1.23+ arm64 binary exists. Tests 491 (+3 gate boundary/fail-closed), types, lint, check-model-registry (0.82.1), check-diagram all green.

## [2026-07-28] update | Rust pin bumped to v0.1.23 (published 2026-07-28), carrying upstream #136 (EBADF write-fd bug on virtiofs) and #139 (first-class `max` thinking level). Bump is `rust-pi-version.json` `tag` only — it moves the managed download (`rust-install.ts`), the README "tested" badge and the untested-binary warning together; the releases API resolves the tag and its assets. `max` REMAINS INERT and the release-time checklist is NOT complete: the two remaining steps are blocked by an unrelated upstream break, recorded here so nobody re-derives it. (1) CATALOG REGEN IMPOSSIBLE AS-IS — `scripts/gen-model-registry.mjs` is built on pi-ai's `getProviders()`/`getModels(id)`, and pi-ai DROPPED both at 0.81.0 (verified by probing 0.81.1 and 0.82.0: `getModels:false getProviders:false`; 0.82.1 errors at import with "does not provide an export named 'getModels'"). The new surface is a store pattern (`createModels`, `InMemoryModelsStore`, `createProvider`). Bundled catalog therefore stays at pi-ai 0.79.8 = 75 `xhigh` keys, ZERO distinct `max` keys, so `getSupportedThinkingLevels` still never offers `max` — which is the designed-safe behaviour, not a bug. Regenerating requires MIGRATING THE GENERATOR to the new API first, and it is still unconfirmed whether any post-0.81 pi-ai actually maps a distinct `max` key. (2) README "off → xhigh" line + CHANGELOG entry stay deferred (unchanged rationale: do not document a level no model can offer). ALSO BLOCKED: v0.1.23 could not be installed or black-box tested on this machine — it is `aarch64` and the release ships no `pi-linux-arm64` asset (only linux-amd64, darwin-arm64, windows-amd64), so an arm64 binary needs a from-source build (`build-patched-pi.sh` in the session scratchpad, which must be run OUTSIDE this Anthropic-backed context per the clean-room wall). The locally installed binary is 0.1.20 — older than the previous v0.1.22 pin — so the untested-binary warning is expected until a matching build is installed. Consequence: #136's EBADF fix is UNVERIFIED here; the rate-limited harness `ebadf-probe.mjs` (also scratchpad) is ready to point at any binary via `--bin`. Verified this pass: 488 tests, tsc (both configs), eslint all green.

## [2026-07-27] update | First-class `max` thinking level (upstream pi_agent_rust#139, implemented-not-yet-released). Promoted `max` to a 7th rung in `THINKING_LEVELS` (`[off,minimal,low,medium,high,xhigh,max]`) and RETIRED `aliasMaxToXhigh` (+ its call in `reconcileThinkingCapability`, which now returns `base` by identity when already-reasoning — same-object contract preserved). Rationale from the data, not a version flag: the bundled catalog (pi-ai 0.79.8) has ZERO `max` KEYS — 14 models key their top tier `xhigh:"max"` (wire value, not key) — so a distinct `max` KEY is itself the signal a post-#139 pi-ai produced it and the backend understands it. `getSupportedThinkingLevels` now gates `max` exactly like `xhigh` (offered only when explicitly mapped), so a pre-#139 catalog never surfaces it; the Rust picker reads bundled metadata only (`findCatalogThinkingModel`), and `aliasMaxToXhigh` only ever ran on the TS-SDK path — so nothing offers/sends `max` to the pinned v0.1.22 binary. One landmine closed: `PiService.supportedThinkingLevels()`'s unresolved-model fallback returned the whole ladder — now filters `max` out (the one level v0.1.22 can't honor, and an unknown model gives no evidence it's supported). `clampThinkingLevel` extends automatically (iterates THINKING_LEVELS). Also: `REASONING_DESCR` gains `max:"maximum reasoning"` and demotes `xhigh` → "very high reasoning"; `package.json` `defaultThinkingLevel` enum gains `max` (a `max` default clamps down gracefully on any current model). Change is INERT today — `max` lights up only when the bundled catalog is regenerated from a post-#139 pi-ai AND the rust pin (`rust-pi-version.json`) is bumped to the release; both are release-time steps. Tests: 488 (rewrote the reconcile/alias tests → first-class-max surfacing + same-object identity; added max gating/clamp/ladder coverage). tsc/eslint/vsix green. README's "off → xhigh" range line and a CHANGELOG entry deliberately deferred until `max` is actually user-selectable (post catalog-regen), to avoid documenting an unreachable level.

## [2026-07-24] update | Webview Frontend — hardened `renderInline()`: escape inline `html` tokens, sanitize link href schemes via `safeLinkHref()`; closes raw-HTML/`javascript:`-link injection in chat rendering

## [2026-07-20] update | Migrated the TS SDK init to pi-coding-agent ≥ 0.80.8 `ModelRuntime` (sdk-resolution.md). 0.80.8 was a breaking redesign — removed the `AuthStorage` export + `ModelRegistry.create(auth)` and unified them into the single async `ModelRuntime`; on the removed API every TS session died at init (`Auth/registry setup failed: Cannot read properties of undefined (reading 'create')`). Root cause was environmental, not ours: the devcontainer installs `@latest`, which rolled 0.80.7 → 0.80.10 (0.80.8/9/10 all published the same day). Tier 1 (commit 5f7e8cb) migrated the session/model path (`await ModelRuntime.create()`, `find`→`getModel`, `getAvailable`/`setRuntimeApiKey`/`refresh` awaited, `getApiKey`→`getAuth().apiKey`, `createAgentSession({ modelRuntime })`; proper rename `authStorage`+`modelRegistry` → `modelRuntime`, no aliases) — verified end-to-end against real 0.80.10. Tier 2 (commit 15ec1b4) reworked `/login`+`/logout` onto `modelRuntime.login(id, "api_key"|"oauth", interaction)` / `listCredentials()` / `logout()`, with `makeAuthInteraction()` adapting the pi-ai `AuthInteraction` (`prompt`/`notify`) to VS Code UI — api-key + logout verified live; **OAuth wired-to-spec but NOT live-verified** (needs a real subscription round-trip — caveat recorded in sdk-resolution.md). Steps 4/5/9/10 of the init sequence updated + new "SDK version compatibility" section (removed→ModelRuntime mapping table). Policy note in the wiki: do NOT pin the SDK to dodge a shape change — adapt the call-sites, the devcontainer stays on @latest. 296 tests / tsc / eslint / vsix green.

## [2026-07-06] update | `PiBackend` seam migration COMPLETE (the tenth-audit's deferred WS3 deep-migration, now carried out) — `SdkService` gains the full primitive surface it was missing (sendPrompt / abort / abortBash / compact / setModel / setThinkingLevel / setAutoCompaction / setAutoRetry / getUsage / getEntries / promoteToSteer / clearQueue); both services now `implements PiBackend` (tsc-enforced; `RustUsage` is structurally `BackendUsage`). PiService gains a `private get backend(): PiBackend | null` accessor and delegates every primitive through it — the paired `_backendKind === "rust"` branches deleted from getDisplayEntries/abort/compact/promoteToSteer/clearQueue/toggles/getUsageStats/setModel/cycleModel/setThinkingLevel/sendPrompt/exportToHtml; the remaining feature gates (thinkingStatus/reportStatus/toggleThinking/pickThinkingLevel/pickActiveTools) read `capabilities`. Removed now-dead `PiService.rustRequestOrError` + the unused rust-process/thinkingLevelIsLive/humanizeProviderError imports (logic lives in the primitives / behind the capability flag). FOUR `_backendKind` checks remain BY DESIGN — runtime identity, not feature gates: the backend accessor + capabilities-fallback, getUsageStats' cost policy (Rust reports cost:0), getAllSlashCommands' agent-command source, dispose's teardown sequencing. Landed as a green 4-commit series (A: SdkService primitives + 10 headless tests / B: simple delegations / C: setModel+setThinkingLevel / D: sendPrompt + capability gates), each tsc/eslint/tests/vsix-green. Docs: runtime-selection.md (status + diagram + "The PiBackend seam" section + ADR note all reflect completion), tdd.md → 277 tests. Two intentional consolidations noted: cycleModel's notice is now uniform (prev → next) across runtimes; the auto-* toggles surface RPC failures as a chat card (consistent with setModel).

## [2026-07-06] update | Tenth-audit symmetry pass — (1) testability: `SdkDeps` injection makes SdkService vscode-free AND json-import-free (config/cwd/module-import/fs/bridge-tools/catalog-providers/UI-nudge all injected); `sdk-service.test.ts` (7 tests) drives the REAL 12-step `SdkService.initialize()` against fake SDK/AI modules — closes both audits' #1 gap (the DEFAULT runtime's init had zero tests). (2) structure: **`PiBackend` interface** (`src/pi-backend.ts`) formalizes the divergent primitives + **`BackendCapabilities`** replaces scattered `_backendKind === "rust"` feature gates (bridgeTools/customCards/toolsPicker/fork/reloadContext/exportHtml/rename/interceptSlashCommands/thinkingLevelLive); both services expose `capabilities` + the primitives; PiService gains a `capabilities` accessor, delegates `exportToHtml`, and gates the /resume,/fork,/export,/tools list on capabilities. Overturns the "internal branching only" ADR (runtime-selection.md updated). (3) correctness: `assertNever(runtime)` exhaustiveness at the init dispatch; shape-drift probe onto a dedicated one-shot `_shapeProbeWarned` (not the transient degradation tracker); proactive missing-fd/rg notice on session start; /compact-differs explanation full-once/terse-after. New page `operations/runtime-debugging.md`; tdd.md → 267 tests / 21 files. Audit inaccuracies rejected: "no rust-process tests" (they exist), "16 vs 17 bridge tools" (16 is correct), RustProcess spawn "unbounded" (readyTimeoutMs bounds it), `Math.floor` "redundant" (it truncates fractions). Known trade-off kept: built-in-model budget parity (H5) — the shadow-entry fix risks replacing sibling provider models.

## [2026-07-06] update | Audit-remediation pass (consolidated ninth dual-runtime audits, both runtimes' findings verified & filtered) — (1) hygiene: temp [usage] diagnostics removed; dead `isTransientProviderError` deleted (binary owns transient retry, gh #118); `parseRustEntries` passes entry-shaped items through (latent compaction-replay bug); `initialized` getter honesty on failed re-init; `get_commands` zero-parse warning; `applyState` per-version fallback annotations; `get_state` init shape probe (pinned-version drift net); models.json write skipped when byte-identical. (2) testability: `RustDeps` injection makes RustService vscode-free; `rust-service.test.ts` (6 tests) drives the REAL init/4-step handshake against a fake rust-pi subprocess; opt-in real-binary contract probe `pnpm run test:rust-smoke`. (3) naming: `rust-catalog.ts` → `model-catalog.ts`; `extractMessageText` + tool-preview helpers moved rust-events → agent-events (import direction now rust-specific → shared). (4) structure: **`SdkService` extracted** (`src/sdk-service.ts`, ~715 LOC) — TS runtime now mirrors RustService; PiService 3,050 → 2,439 lines, SDK fields become getters over `_sdk`. Pages: runtime-selection.md (diagram + inventory), pi-service.md, tdd.md (260 tests / 20 files). Rejected-with-reasons: `satisfies RustHost` (already type-checked), queue UUIDs (no metadata channel; needs upstream queue_update), THINKING_LIVE_TRANSPORTS data-driving (verification discipline), README "16 tools" drift claim (count verified correct).
## [YYYY-MM-DD] <action> | <description>`. Actions:
`ingest` (new page), `update` (existing page changed), `lint` (quality
pass performed), `archive` (page moved to archive).

## [2026-06-25] ingest | Error Surfacing (`operations/error-surfacing.md`) — pure `src/extension-errors.ts` classifiers (`classifyProviderConfigError`, `classifyRustLoadError` + humanize/format helpers) that turn raw failures into clear diagnostics. TS provider-key failures (e.g. the obsolete `$ENV:NAME` config syntax → "from environment variable: ENV") are humanized at the `agent-events.ts` error choke point + the pi-service steer-reject path; Rust extension-load failures (digest-mismatch / unsupported-module / load-failed) are deduped in `RustProcess` and surfaced once as an in-chat card via `RustService`, ending the `[rust-stderr]` flood. tdd.md → 199 tests / 18 files (added `extension-errors`); logging.md cross-linked + `[rust-stderr]` row noted as no longer always verbatim; index.md updated.
## [2026-06-25] ingest | Logging policy — new `operations/logging.md`: all logging routes solely through the "Pi Code Gui" `LogOutputChannel` (no shared-console writes); `piDebug`/`piLog`/`piWarn` level discipline with routine lifecycle chatter demoted to `debug` so the channel is quiet at the default Info level; teardown-safe `write()` guard; `logger.test.ts` (5 tests incl. a never-touches-`console.*` guard). Linked from index.md. event-translation.md documents `normalizeToolArgs` (always coerces `tool-start` `data.args` to a record across all three emit sites — fixes null-args validation noise on historical/Rust replay). tdd.md → 190 tests / 17 files.
## [2026-06-25] update | Phase 6 hardening docs — tdd.md (182 tests / 16 files; documented the jsdom dispatch seam via `tsconfig.test-webview.json` + variable-specifier import); webview-frontend.md (inbound `validateWebviewToExtension` now wired warn-only at `onDidReceiveMessage`; schema 38 events / 18 commands after adding `switchRuntime`)
## [2026-06-24] update | Event Translation — extracted the dispatcher's decision logic into the pure `translateAgentEvent` (`src/agent-events.ts`, returns events + mutations + effects) with a 36-test per-event-type suite (`src/test/unit/agent-events.test.ts`); `handleAgentEvent` is now a thin apply/emit/effects shell. tdd.md test count → 123 across 7 files.
## [2026-06-24] lint | Phase-0 drift sweep — tdd.md (82 headless `node:test` unit tests via `pnpm run test:unit`, extract-then-test pattern; killed "single skeleton test"); runtime-selection.md (`RustService.handleEvent`→`normalizeRustEvent`/`routeRustEvent`→`RustHost.handleAgentEvent`; added `src/rust-service.ts`+`src/rust-events.ts` to file inventory); streaming-pipeline.md (200ms `TOOL_PREVIEW_THROTTLE_MS` tool-arg throttle, commit 8322a4d); webview-frontend.md (38 events/17 commands; removed false "webview→extension is Zod-validated" claim — inbound `validateWebviewToExtension` is unwired); tree-views.md (banner images non-functional, Rust "Update All" false-positive); session-window.md (single restore loop in `activate()`, no `restoreAdditionalSessions()`/`continueRecent`); index.md + bootstrap/AGENTS.md (16 bridge tools, not 17; moved Component System Proposal out of Roadmap)
## [2026-06-05] update | Tree Views + Runtime Selection — runtime-aware Packages view: shared catalog, focus-driven available-vs-active (`computeActiveSources`/`setFocusedRuntime`), Rust binary backend (`src/rust-packages.ts`: list/install/remove/update/info/doctor), install-time Rust-compat warning, provenance/safety signals
## [2026-06-05] update | Runtime Selection — interop caveat resolved: `--no-extensions` + `pi-code-gui.rustExtensions` setting (auto/enabled/disabled), `shouldDisableRustExtensions`/`workspaceHasTsPiExtensions`/`isRustExtensionConflict`, spawn-retry self-heal + actionable conflict dialog
## [2026-06-05] ingest | Architecture — Runtime Selection (`architecture/runtime-selection.md`): TypeScript + Rust runtimes, `backendKind` internal branching, RPC plumbing, detection/install, documented Rust limitations
## [2026-06-05] ingest | Architecture — Runtime Switching UX (`architecture/runtime-switching-ux.md`): commands, persisted default, status chip + tree badges, setContext gating, resume-follows-origin, mixed-runtime tabs
## [2026-06-05] update | Archive — `archive/multi-backend.md` marked superseded (2025 rejection reversed; dual-runtime shipped in v0.0.56 with markdown-fallback for custom cards)
## [2026-06-05] update | Index — added Runtime Selection + Runtime Switching UX under Architecture
## [2026-05-27] lint | Full wiki + README audit — bridge-tools (16 tools, not 17; dedup table row), syntax-highlighting (15 languages, +yaml/sql/diff), tree-views (progressive load, entry caching, state-change refresh), event-translation (turn-end, message_update error, user message_end), streaming-pipeline (progressive replay), README (16 tools)
## [2026-05-26] update | Bridge Tools, PiService — added `/tools` runtime picker with persistence, replaced static `pi-code-gui.tools` allowlist
## [2026-05-25] archive | Architecture — Multi-Backend Architecture → `archive/multi-backend.md` (rejected: incompatible extension/UI model)
## [2026-05-25] ingest | Architecture — Multi-Backend Architecture (`architecture/multi-backend.md`)
## [2026-05-19] update | Webview Frontend — Steps 6-7 complete (interactive dialogs, persistent status bar); Component System Proposal all 7 steps done
## [2026-05-19] update | Webview Frontend — Layer 3 micro components (CodeBlock, ThinkingBlock, LiveCard, InlineCard, ToolBlock); Component System Proposal Layers 1-3 complete
## [2026-05-19] update | Webview Frontend — Layer 2 safe HTML tagged template; Component System Proposal Layers 1+2 marked complete
## [2026-05-19] update | Webview Frontend — Layer 1 Zod runtime protocol validation; Component System Proposal Layer 1 marked complete
## [2026-05-19] ingest | Architecture — Syntax Highlighting (`architecture/syntax-highlighting.md`)
## [2026-05-19] ingest | Architecture — Custom Message Renderer (`architecture/custom-message-renderer.md`)
## [2026-05-19] ingest | Architecture — Tool Block Rendering (`architecture/tool-block-rendering.md`)
## [2026-05-19] ingest | Architecture — Streaming Pipeline (`architecture/streaming-pipeline.md`)
## [2026-05-19] ingest | Roadmap — Component System Proposal (`architecture/component-system-proposal.md`)
## [2026-05-19] update | Webview Frontend — added highlight.js, streaming pipeline, component proposal cross-refs
## [2026-05-19] update | Extension UI Bridge — added custom message renderer bridge, HTML support in setStatus, globalThis injection
## [2026-05-19] update | Event Translation — added custom message display/details fields, diagnostic default case
## [2026-05-19] update | Index — added Syntax Highlighting, Tool Block Rendering, Streaming Pipeline, Custom Message Renderer, Roadmap category
## [2026-05-16] archive | Webview Rewrite Plan moved to archive/ (all 5 steps completed)
## [2026-05-16] stale | Removed WEBVIEW_REWRITE_TODO.md (tracking complete)
## [2026-05-16] lint | Fixed all 65 ESLint warnings in src/webview — 0 warnings remain
## [2026-05-16] update | Webview Panel + Frontend — CSS extracted to media/style.css, @layer organization, native nesting
## [2026-05-16] update | Webview Panel — updated for single-bundle loading via esbuild, typed postMessage bridge
## [2026-05-16] update | Webview Rewrite Plan — marked all 5 steps complete, final architecture documented
## [2026-05-16] stale | Removed media/app.js, media/core.js, media/tools.js (migrated to src/webview/)
## [2026-05-16] ingest | Webview Rewrite Plan — 5-step modularization + TS migration, TODO tracking file
## [2026-05-15] ingest | Bootstrap — wiki structure initialized from Pi template (discipline pages, index, log, archive)
## [2026-05-15] ingest | Architecture — Session Window (`architecture/session-window.md`)
## [2026-05-15] ingest | Architecture — PiService (`architecture/pi-service.md`)
## [2026-05-15] update | PiService — added pickModel/pickThinkingLevel, de-duplicated pickers from extension.ts and webview-panel.ts, added model pricing display
## [2026-05-15] ingest | Architecture — Webview Panel (`architecture/webview-panel.md`)
## [2026-05-15] ingest | Architecture — Bridge Tools (`architecture/bridge-tools.md`)
## [2026-05-15] ingest | Architecture — Event Translation (`architecture/event-translation.md`)
## [2026-05-15] ingest | Architecture — Extension UI Bridge (`architecture/extension-ui-bridge.md`)
## [2026-05-15] ingest | Architecture — Webview Frontend (`architecture/webview-frontend.md`)
## [2026-05-15] ingest | Architecture — Tree Views (`architecture/tree-views.md`)
## [2026-05-15] ingest | Operations — SDK Resolution & Init (`operations/sdk-resolution.md`)
## [2026-05-15] ingest | Operations — Build Pipeline (`operations/build-pipeline.md`)
## [2026-05-15] update | AGENTS.md bootstrap — filled project overview, dev workflow, tool discipline, storage rules, quick reference
## [2026-05-15] update | TDD discipline — filled project test conventions (locations, commands, preflight)

## [2026-08-31] audit | Full audit against CHANGELOG 0.1.3-0.2.0 — the wiki was ~9 releases behind
## [2026-08-31] ingest | Architecture — Model Catalog & the Shared Agent Home (`architecture/model-catalog.md`)
## [2026-08-31] ingest | Architecture — Session Modes (`architecture/session-modes.md`)
## [2026-08-31] update | Runtime Selection — CORRECTED: the relocated agent home and linked `auth.json` it described were removed in 0.2.0; RPC flags re-verified against v0.3.0; noted the inert approval flags and the blocking `ask` tool
## [2026-08-31] update | PiBackend — plan/approval are extension-owned session state
## [2026-08-31] update | Extension UI Bridge — `ask_request`/`ask_response` contract; the three dialog faults it exposed
## [2026-08-31] update | SDK Resolution — the PATH-entry candidate (#81)
## [2026-08-31] update | Build Pipeline — release/publish chain, tag-ref requirement, absent approval gate, publish-time supply-chain scan
## [2026-08-31] update | Error Surfacing — provenance package naming; severity matched to intent
## [2026-08-31] update | Webview Panel — panel column (#83) and the mode strip
## [2026-08-31] update | Syntax Highlighting — corrected the language count (15 -> 14) against source
## [2026-08-31] update | Tool Block Rendering — audited, accurate, footer refreshed
## [2026-08-31] update | Promoted 9 pages from "evolving" to "stable" after verifying each; the two ingested today stay evolving
## [2026-08-31] update | wiki-lint — staleness and status-rotation checks now skip `discipline/`: process pages have no code to drift from, and five false positives were hiding the one page that was genuinely wrong
## [2026-08-31] update | Runtime Selection — corrected the stale `_backendKind` list and count; noted `sessionModes` as the first true-for-Rust capability
